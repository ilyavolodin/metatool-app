DATABASE_URL=sqlite.db
USE_DOCKER_HOST=false # Not typically needed for SQLite local dev; can be true if sub-processes need to reach host services
# REMOTE_HOSTING_URL is no longer needed as remote-hosting is merged.
METAMCP_API_BASE_URL=http://localhost:12005 # For sub-MCP processes to callback to the main app
NEXT_PUBLIC_APP_URL=http://localhost:12005 # Base URL of this application
NEXT_PUBLIC_REMOTE_HOSTING_URL=http://localhost:12005 # For client-side code needing absolute URL to backend (now itself)

